export function myOwnAdd(a, b) {
  return a + b;
}

export function myOwnMultiply(a, b) {
  return a * b;
}

// export default може бути лише один на файл
export default function myOwnAdd1(a, b) {
  return a + b;
}
